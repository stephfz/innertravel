<%= form_for(@trip) do |f| %>
  <% if @trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trip.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
      <% @trip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :departing %><br />
    <%= f.text_field :departing %>
  </div>
  <div class="field">
    <%= f.label :returning %><br />
    <%= f.text_field :returning %>
  </div>
  <div class="field">
    <%= f.label :setsavailable %><br />
    <%= f.check_box :setsavailable %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
